// Stub — replaced in Group G1.
interface ThreadPanelProps {
  chatId: string;
  parentMessageId: string;
}

export function ThreadPanel({ chatId: _chatId, parentMessageId: _parentMessageId }: ThreadPanelProps) {
  return <aside style={{ borderLeft: '1px solid var(--border-0)' }}>thread stub</aside>;
}
