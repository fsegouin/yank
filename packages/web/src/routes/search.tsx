import { createFileRoute } from '@tanstack/react-router';

export const Route = createFileRoute('/search')({
  component: () => <main style={{ padding: 24 }}>Search stub — Group E6 fills it.</main>,
});
